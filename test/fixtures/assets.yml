# == Schema Information
# Schema version: 52
#
# Table name: assets
#
#  id                :integer(11)     not null, primary key
#  type              :text            
#  created_at        :datetime        
#  updated_at        :datetime        
#  position          :integer(11)     
#  creator_id        :integer(11)     
#  source_uri        :string(255)     
#  original_filename :string(255)     
#

# This empty file is so that when we do db:fixtures:load from
# our project, we erase records that were added into tables
# defined in a plugin
